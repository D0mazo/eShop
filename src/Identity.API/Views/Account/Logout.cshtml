@model LogoutViewModel

<div class="logout-page">
    <div class="lead">
        <h1>Atsijungti</h1>
        <p>Norite Atsijungti?</p>
    </div>

    <form asp-action="Logout">
        <input type="hidden" name="logoutId" value="@Model.LogoutId"  />
        <div class="form-group">
            <button class="btn btn-primary">Taip</button>
        </div>
    </form>
</div>
